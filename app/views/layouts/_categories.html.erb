<%if categories%>
    <h5>Categories</h5>
    <% categories.each do|category|%>
    <ul>  
        <li><%= link_to category.name, category_search_path(category.name)%>(<%= category.posts.size%>) </li>
    </ul>
    <%end%>
<%end%>